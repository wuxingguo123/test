# 剩磁衰减动态模型构建与 Day 23–29 预测

## 1. 问题分析

本问题的核心目标是根据铁磁材料模拟雷击后的剩磁观测数据，建立剩磁随时间衰减的动态模型，并结合逐日温度、湿度等环境数据解释不同时间段衰减速率的变化。在此基础上，需要对实验中缺失的 Day 23–29 进行预测，最终输出每类样品、每个编号样品在该时间段内的剩磁估计值。

数据分析表明，同一类型样品在同一测量时刻的归一化剩磁比 $B(t)/B_0$ 高度一致。因此，本文将剩磁衰减过程分解为

$$
B_{i,c}(t)=B_{i,c}(0)\cdot D_c(t),
$$

其中，$B_{i,c}(0)$ 表示第 $c$ 类第 $i$ 个样品的初始剩磁，$D_c(t)$ 表示第 $c$ 类样品共有的归一化衰减函数。该结构将不同编号样品的初始剩磁差异与样品类型层面的公共衰减规律分离开来，使问题由“每个样品单独建模”转化为“每类样品统一建模”。这一处理消除了初始剩磁大小差异对衰减规律识别的干扰，提高了参数估计稳定性，也为缺失时间段预测提供了统一的归一化尺度。

从归一化衰减统计结果看，小号铁钉 在 Day 0–58 的最大 CV 为 0.1296%，全时段平均 CV 为 0.1090%，全时段最大 CV 为 2.2237%；小号铁夹 在 Day 0–58 的最大 CV 为 0.1368%，全时段平均 CV 为 0.1348%，全时段最大 CV 为 2.7893%；普通钢筋 在 Day 0–58 的最大 CV 为 0.0352%，全时段平均 CV 为 0.0187%，全时段最大 CV 为 0.3131%；锈蚀钢筋 在 Day 0–58 的最大 CV 为 0.1984%，全时段平均 CV 为 0.2987%，全时段最大 CV 为 6.4959%。其中末期 Day 90 剩磁值已接近零，少量绝对误差会被相对 CV 放大，因此分离性判断更应结合主要衰减阶段和绝对误差共同理解。该结果说明，分离模型并非单纯假设，而是由实验数据支持的结构性特征。

## 2. 模型假设

**假设 1：实验测得的 Day 0 剩磁值即为该样品的初始剩磁 $B_0$。**  
模拟雷击后 Day 0 的观测值反映样品受到雷击磁化后的初始状态，因此可直接作为后续归一化和预测的基准。

**假设 2：同类型样品具有相同的归一化衰减函数 $D_c(t)$。**  
同一类型样品在尺寸、形状、材料和锈蚀状态上相同或相近，实验结果也显示其 $B(t)/B_0$ 曲线高度一致，因此个体差异主要体现在 $B_0$ 上。

**假设 3：在相邻两天之间，剩磁衰减可以用指数衰减描述。**  
铁磁材料剩磁衰减与磁弛豫过程有关，短时间步长内可用 $B(t+1)=B(t)\exp[-\lambda(t)]$ 表示，进而得到累计指数衰减形式。

**假设 4：每日衰减率 $\lambda_c(t)$ 受到当天温度和湿度影响，并且始终为正。**  
温度会影响磁畴弛豫过程，湿度可能通过氧化、腐蚀等方式改变材料表面状态。采用指数形式刻画 $\lambda_c(t)$，可以保证每日衰减率为正。

**假设 5：天气表中序号 $k$ 的环境数据用于 Day $k-1$ 到 Day $k$ 的衰减过程。**  
例如 Day 22 到 Day 23 使用天气序号 23，Day 29 到 Day 30 使用天气序号 30。该约定保证非连续观测区间能够被正确拆分为逐日衰减贡献。

**假设 6：Day 22 和 Day 30 的实验观测可靠。**  
Day 23–29 位于两个真实观测边界之间，因此预测时不仅要体现环境因素对每日衰减快慢的影响，还要保证预测曲线与 Day 22 和 Day 30 的观测值精确衔接。

## 3. 符号说明

| 符号 | 含义 |
| --- | --- |
| $i$ | 样品编号 |
| $c$ | 样品类型 |
| $t$ | 实验天数 |
| $B_{i,c}(t)$ | 第 $c$ 类第 $i$ 个样品在第 $t$ 天的剩磁 |
| $B_{i,c}(0)$ | 第 $c$ 类第 $i$ 个样品的初始剩磁 |
| $D_c(t)$ | 第 $c$ 类样品的归一化衰减函数 |
| $\lambda_c(t)$ | 第 $c$ 类样品在第 $t$ 天的每日衰减率 |
| $T_t$ | 第 $t$ 天温度 |
| $H_t$ | 第 $t$ 天相对湿度 |
| $\theta_{0,c},\theta_{T,c},\theta_{H,c}$ | 环境驱动衰减率模型参数 |
| $L_j$ | 第 $j$ 个观测区间的累计对数衰减 |

## 4. 模型建立

### 4.1 分离模型

对每个样品，先以 Day 0 的剩磁值作为初始剩磁 $B_{i,c}(0)$，计算归一化剩磁比：

$$
D_{i,c}(t_k)=\frac{B_{i,c}(t_k)}{B_{i,c}(0)}.
$$

由于同类型样品的归一化衰减比高度一致，可用同类样品平均值估计该类样品的公共衰减函数：

$$
D_c(t_k)=\frac{1}{n_c}\sum_i \frac{B_{i,c}(t_k)}{B_{i,c}(0)}.
$$

于是每个样品在任意时刻的剩磁可表示为：

$$
B_{i,c}(t)=B_{i,c}(0)D_c(t).
$$

### 4.2 指数衰减模型

从磁弛豫理论出发，剩磁在相邻两天之间可表示为指数衰减：

$$
B(t+1)=B(t)\exp[-\lambda(t)].
$$

归一化后得到：

$$
D_c(t)=\exp\left[-\sum_{s=1}^t \lambda_c(s)\right].
$$

因此，$D_c(t)$ 的变化可以由每日衰减率 $\lambda_c(t)$ 的累计效应解释。

### 4.3 区间累计对数衰减

对于相邻观测点 $t_a$ 和 $t_b$，定义观测区间累计对数衰减：

$$
L_c(t_a,t_b)=-\ln\frac{D_c(t_b)}{D_c(t_a)}.
$$

根据指数衰减递推式，该区间累计对数衰减等于区间内每日衰减率之和：

$$
L_c(t_a,t_b)=\sum_{d=t_a+1}^{t_b}\lambda_c(d).
$$

这一处理尤其重要，因为实验观测天数并不完全连续，例如 Day 15→22、Day 22→30 均为多日区间。如果直接将其当作单日衰减，会忽略区间内逐日天气变化对衰减率的影响。

### 4.4 环境驱动每日衰减率模型

本文采用正值指数形式刻画每日衰减率：

$$
\lambda_c(d)=\exp\left(\theta_{0,c}+\theta_{T,c}\frac{T_d}{T_{ref}}+\theta_{H,c}\ln\frac{H_d}{H_{ref}}\right),
$$

其中 $T_{ref}=10$，$H_{ref}=80$。指数形式保证 $\lambda_c(d)>0$，符合剩磁衰减率非负的物理要求。参数 $\theta_{T,c}$ 表征温度对衰减速率的影响，$\theta_{H,c}$ 表征湿度对衰减速率的影响，不同样品类型分别拟合参数，以反映材料、尺寸、形状和锈蚀状态差异。

### 4.5 参数估计

对每类样品，利用所有相邻观测区间的累计对数衰减进行非线性最小二乘估计：

$$
\min \sum_j \left[L_{obs,j}-L_{pred,j}\right]^2,
$$

其中

$$
L_{pred,j}=\sum_{d=t_{start,j}+1}^{t_{end,j}}\lambda_c(d).
$$

这种方法不是对相邻观测点简单差分，而是将非连续观测区间内的逐日天气数据全部纳入累计衰减计算，因此能够更稳健地利用实验数据。

模型参数估计结果为：小号铁夹：theta0=-2.606，thetaT=0.1037，thetaH=5.339，lambda_baseline=0.08187；小号铁钉：theta0=-2.667，thetaT=0.1086，thetaH=5.337，lambda_baseline=0.07739；普通钢筋：theta0=-2.873，thetaT=0.1056，thetaH=5.288，lambda_baseline=0.06285；锈蚀钢筋：theta0=-2.486，thetaT=0.1027，thetaH=5.371，lambda_baseline=0.09228。普通钢筋的基准衰减率为 0.06285，锈蚀钢筋为 0.09228，锈蚀样品的基准衰减水平约为普通钢筋的 1.468 倍。

## 5. Day 23–29 预测方法

Day 22 和 Day 30 均有真实观测，Day 23–29 位于两个已知边界之间。直接用环境模型递推可能因参数拟合误差导致 Day 30 的预测值与真实观测不完全一致。为此，本文采用“环境权重分配 + 边界校准”的策略。

首先计算 Day 22 到 Day 30 的真实累计衰减：

$$
L_{22,30}=-\ln\frac{D_c(30)}{D_c(22)}.
$$

然后用环境模型计算 Day 23–30 的原始每日衰减率：

$$
\lambda_{raw}(d),\quad d=23,24,\ldots,30.
$$

原始累计衰减为：

$$
L_{raw}=\sum_{d=23}^{30}\lambda_{raw}(d).
$$

为保证 Day 30 边界精确满足，引入缩放系数：

$$
s=\frac{L_{22,30}}{L_{raw}},
$$

并得到校准后的每日衰减率：

$$
\lambda_{cal}(d)=s\lambda_{raw}(d).
$$

最后从 Day 22 开始逐日递推：

$$
D_c(t)=D_c(t-1)\exp[-\lambda_{cal}(t)],\quad t=23,\ldots,29,
$$

并还原到每个样品：

$$
B_{i,c}(t)=B_{i,c}(0)D_c(t).
$$

该方法既保留了环境因素对每日衰减快慢的相对影响，又严格满足 Day 22 和 Day 30 两个真实观测边界，使 Day 23–29 的预测同时具有物理解释性和数据一致性。

## 6. 模型验证与结果分析

### 6.1 分离模型验证

为验证 $B=B_0D_c(t)$ 的分离模型，本文采用样品层面的留一交叉验证。对每一类样品，每次留出一个样品，用同类型其余样品计算 $D_c(t)$，再用被留出样品的 $B_0$ 预测其全部观测日剩磁，并与真实观测值比较。

评价指标包括 MAE、RMSE、MAPE 和 $R^2$。其中 MAE 和 RMSE 衡量预测误差的绝对大小，MAPE 衡量相对误差，$R^2$ 衡量预测值对真实值变化的解释能力。整体 LOOCV 结果为 MAE=2.719e-05 mT，RMSE=3.297e-05 mT，MAPE=0.12%，R²=1。该结果说明，分离模型能够非常稳定地恢复样品层面的剩磁观测值。

### 6.2 环境模型验证

环境模型的验证主要考察模型预测的区间累计 log 衰减与真实累计 log 衰减是否一致。根据结果文件，小号铁夹 的 R²=0.9954、RMSE=0.0435；小号铁钉 的 R²=0.9955、RMSE=0.04089；普通钢筋 的 R²=0.9956、RMSE=0.03287；锈蚀钢筋 的 R²=0.9953、RMSE=0.04957。这表明温度、湿度驱动的每日正值衰减率模型能够较好解释不同观测区间衰减速率的变化。

进一步地，将拟合模型沿观测时间轴重构 $D_c(t)$，得到：小号铁夹 的 D 重构 R²=0.9843、MAPE=10.40%；小号铁钉 的 D 重构 R²=0.9847、MAPE=9.77%；普通钢筋 的 D 重构 R²=0.9857、MAPE=7.99%；锈蚀钢筋 的 D 重构 R²=0.9839、MAPE=11.72%。由于观测时间点数量有限，且温度与湿度在自然环境下可能存在相关性，环境模型参数仍存在一定不确定性；同时，材料微观结构、表面氧化程度和雷击磁化强度差异等未观测因素也可能影响部分区间误差。

### 6.3 参数解释

$\theta_0$ 反映基准衰减水平，$\theta_T$ 反映温度敏感性，$\theta_H$ 反映湿度敏感性。当 $\theta_T>0$ 时，说明温度升高会提高每日衰减率；当 $\theta_H>0$ 时，说明相对湿度升高会加快剩磁衰减。若某些参数出现负值，则需要结合样本范围、温湿度相关性和实验误差谨慎解释。

从已保存参数看，四类样品的 $\theta_T$ 与 $\theta_H$ 均为正，说明在本实验环境范围内，高温和高湿整体上对应更快的剩磁衰减。锈蚀钢筋的基准衰减水平高于普通钢筋，说明锈蚀状态降低了剩磁稳定性，这与锈蚀层改变材料表面状态和磁畴钉扎条件的物理认识一致。

### 6.4 Day 23–29 预测分析

根据预测结果，小号铁钉 的预测均值由 Day 23 的 0.4970 mT 下降至 Day 29 的 0.3155 mT；小号铁夹 的预测均值由 Day 23 的 0.4338 mT 下降至 Day 29 的 0.2683 mT；普通钢筋 的预测均值由 Day 23 的 0.9015 mT 下降至 Day 29 的 0.6235 mT；锈蚀钢筋 的预测均值由 Day 23 的 0.5770 mT 下降至 Day 29 的 0.3358 mT。同类型样品的预测曲线形状一致，这是因为它们共享同一归一化衰减函数 $D_c(t)$；但绝对剩磁值不同，这是由各样品初始剩磁 $B_0$ 的差异造成的。

## 7. 模型优点与不足

本文模型的主要优点包括：第一，充分利用了同类型样品归一化衰减比高度一致的关键数据结构；第二，将个体初始剩磁差异和类型层面的衰减规律分离，提高了建模稳定性；第三，通过指数衰减形式保证每日衰减率为正，并使剩磁随时间单调衰减；第四，在处理非连续观测区间时引入逐日天气数据，避免将多日区间误作单日衰减；第五，在 Day 23–29 预测中引入边界校准，保证预测曲线与真实观测边界一致；第六，模型参数具有明确的物理解释。

模型也存在一定不足：实验观测时间点数量有限，温度和湿度之间可能存在相关性，模型未显式考虑材料微观结构、表面氧化程度、雷击磁化强度差异等因素；环境模型参数可能存在不确定性；缺失区间预测依赖 Day 22 和 Day 30 观测值的可靠性。

## 8. 小结

本文从实验数据出发，首先发现同类型样品归一化剩磁衰减比高度一致，进而建立了 $B(t)=B_0D_c(t)$ 的分离模型。随后结合磁弛豫理论，构建环境驱动的正值指数衰减率模型，并利用相邻观测区间的累计对数衰减进行参数估计。对于 Day 23–29 的缺失数据，本文采用环境权重分配与边界校准相结合的方法，在保证 Day 22 和 Day 30 真实观测约束的同时，得到具有物理解释性的逐日预测结果。
